//common string methods with argument
let email='adeosunfavoradewole@gmail.com';

// let result=email.lastIndexOf('n');

// let result=email.slice(7,12);

// let result=email.substr(4,10);

// let result=email.replace('f', 'v');

// let result=email.replace('a', 'ah');   //it will only replace the first letter 'a' but others won't be replaced.

console.log(result);